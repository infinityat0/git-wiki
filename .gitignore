node_modules/
dist/
.DS_Store
.env

# Docs *content* to render is a separate repo, cloned here at runtime — never commit it.
repo-cache/

# NOTE: docs/ (our ADRs, specs, designs, plans) IS tracked — do not ignore it.
